node_modules/
.env
service-account.json
client_secret*.json
client/dist/
package-lock.json

# Local-only artifacts (not committed)
static/                           # favicons, hygiene batch reports, local-only assets
state/                            # runtime watermarks + agenda cache (gmail history, agenda-cache.json, etc.)
backups/                          # Qdrant snapshot files (local rotation, last 3)
brain-hygiene-batch-*.md          # in case a report lands at repo root by accident

